#include "main.h"
#include <string.h>

/**
 * _strlen - prints the length of string
 *
 * @s : a string
 *
 * Return: the lenght of string s
 */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
